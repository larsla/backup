
Backup server script
 by lars.la@gmail.com

Copy backup.php to a web directory, like /var/www/.
Edit it and change BACKUPDIR, timezone, mailto.
Edit /etc/php5/apache2/php.ini and change to the following settings:
	post_max_size = 500M
	upload_max_filesize = 500M
Save the file and restart apache2.
